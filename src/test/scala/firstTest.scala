import org.scalatest.funsuite
import org.scalatest.funsuite.AnyFunSuite
import csv.Project0

class firstTest extends AnyFunSuite {
  test("Output testing for carrot()") {
    // assert(Project0.carrot() == "carrot")
    // assert(!Project0.carrot().isEmpty())
  }
}
